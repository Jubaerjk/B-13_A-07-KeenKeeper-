/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,

  images: {
    domains: ["randomuser.me", "images.unsplash.com"],
  },
};

export default nextConfig;
